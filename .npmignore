src/
# Compiled tests. `npm test` runs them from dist/test/ in the WORKING TREE,
# which .npmignore does not touch — this only keeps them out of the published
# tarball, where they were ~30% of the unpacked size and nothing imports them.
dist/test/
dist-panel-test/
.github/
.mf/
*.tsbuildinfo
.DS_Store
eslint.config.ts
vite.config.ts
tsconfig.json

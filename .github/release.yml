# What GitHub's release-notes generator leaves out. release-please builds
# each release's notes with it (changelog-type: github), so its own release
# PR — merged right before the tag — would otherwise list itself.
changelog:
  exclude:
    labels:
      - "autorelease: pending"
      - "autorelease: tagged"
